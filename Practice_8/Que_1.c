#include<stdio.h>

int main(){
    // char str[] = {'P', 'R', 'E', 'M', '\0'}; 
    // // char str[] = "PREM";
    // char *ptr = &str[0];
    // while(*ptr!='\0'){
    //     printf("%c",*ptr);  
    //     ptr++;
    // }

char st[0];
    gets(st);
    puts(st);
    // printf("You entered %s ",st);
    return 0;
}